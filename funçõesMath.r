c <- 25
sqrt(c) #raíz quadrada
factorial(c)
y <- -6
sqrt(y)
abs(c) #Valor Absoluto
log(c, base = 2) # -> Logaritmos

z <- log(c, base = 2)
round(z, digits = 2) #Arredondamento (no caso para duas casas decimais)
signif(z, digits = 2)  # Números Significativos (Paricido com a função round) Número de digitos que vai aparecer no parâmetro

